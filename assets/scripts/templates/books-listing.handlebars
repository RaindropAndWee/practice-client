{{#each books as |book|}}

 <!-- Developers, why are we using data-id and not just id? -->
<div style="padding: 30px;">
      <h1 data-id="{{book.id}}">
          {{book.title}}
      </h1>
      <h3 style="margin-top: 0;">
        {{book.author}}
      </h3>

</div>
{{/each}}
